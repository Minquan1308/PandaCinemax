@* @model DoAnCoSoTL.Models.Screening

@{
    Layout = "_AdminLayout";
}

<div class="container mt-5">
    <h2>Screening Details</h2>
    <hr />

    <dl class="row">

        <dt class="col-sm-3">Movie Name:</dt>
        <dd class="col-sm-9">@Model.Movie.Name</dd>

        <dt class="col-sm-3">Cinema Name:</dt>
        <dd class="col-sm-9">@Model.Cinema.Name</dd>

        <dt class="col-sm-3">Date:</dt>
        <dd class="col-sm-9">@Model.Date.ToShortDateString()</dd>

        <dt class="col-sm-3">Start Time:</dt>
        <dd class="col-sm-9">@Model.Time</dd>

        <dt class="col-sm-3">End Time:</dt>
        <dd class="col-sm-9">@Model.EndTime</dd>
    </dl>

    <div class="form-group">
        <a asp-action="Index" class="btn btn-secondary">Back</a>
    </div>
</div>
 *@